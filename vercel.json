{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "yarn --cwd app/web install --frozen-lockfile",
  "buildCommand": "yarn --cwd app/web build",
  "outputDirectory": "app/web/.next"
}
