# Pixl

Pixl is a Solana and MagicBlock pixel world.

## Commands

Build the program:

```bash
anchor build
```

Run all tests against the running Surfpool instance:

```bash
yarn test:all
```

Run individual test files:

```bash
yarn test:init-game
yarn test:init-player
yarn test:start-season
```
