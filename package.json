{
  "license": "ISC",
  "scripts": {
    "test:end-season": "ts-mocha -p ./tsconfig.json -t 300000 tests/end-season.ts",
    "test:init-game": "ts-mocha -p ./tsconfig.json -t 300000 tests/init-game.ts",
    "test:init-player": "ts-mocha -p ./tsconfig.json -t 300000 tests/init-player.ts",
    "test:join-season": "ts-mocha -p ./tsconfig.json -t 300000 tests/join-season.ts",
    "test:paint-pixel": "ts-mocha -p ./tsconfig.json -t 300000 tests/paint-pixel.ts",
    "test:start-season": "ts-mocha -p ./tsconfig.json -t 300000 tests/start-season.ts",
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "dotenv": "^17.4.2",
    "mocha": "^9.0.3",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.7.3"
  }
}
