# Solana L1 (base layer) RPC — wallet txns, delegation, settlement.
# Falls back to https://api.devnet.solana.com if unset.
NEXT_PUBLIC_L1_RPC=https://api.devnet.solana.com

# MagicBlock Ephemeral Rollup RPC — delegated paints, canvas/player state.
# Falls back to https://devnet.magicblock.app if unset.
NEXT_PUBLIC_ER_RPC=https://devnet.magicblock.app
